[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["chmod +x render-build.sh", "./render-build.sh"]

[start]
cmd = "cd backend && node dist/index-pg.js"
