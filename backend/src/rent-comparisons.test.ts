import {expect,it} from 'vitest';
import * as XLSX from 'xlsx';
import {crmRentSamples,parseOnsRents,streetKey} from './rent-comparisons';
it('matches full postcodes and street names, not unrelated roads with the same house number',()=>{const rows=[{address:'21a Northwood Park Road, Wolverhampton',postcode:'WV10 8EU',monthly_rent:800},{address:'22a Northwood Park Road',postcode:'WV10 8ET',monthly_rent:650},{address:'21a Other Road',postcode:'WV1 1AA',monthly_rent:1000}];expect(streetKey(rows[0].address)).toBe('northwood park road');expect(crmRentSamples(rows[0],rows)).toEqual({postcode:{count:1,average:800},street:{count:2,average:725}});});
it('parses ONS latest month per area and preserves missing breakdowns',()=>{const rows:any[]=[['Time period','Area code','Area name','Rental price','Annual change','Rental price one bed']];for(let i=0;i<100;i++){rows.push([46023,'E'+i,'Area '+i,800,4,'[x]']);rows.push([46054,'E'+i,'Area '+i,810,4.2,700]);}const w=XLSX.utils.book_new();XLSX.utils.book_append_sheet(w,XLSX.utils.aoa_to_sheet(rows),'Table 1');const data=parseOnsRents(XLSX.write(w,{type:'buffer',bookType:'xlsx'}));expect(data.E0.average).toBe(810);expect(data.E0.bedrooms.one).toBe(700);expect(data.E0.property_types.flat).toBeNull();});
